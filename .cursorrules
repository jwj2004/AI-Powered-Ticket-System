# 知答 · Cursor 项目规则（企业知识库问答 Agent）

## 项目是什么
「知答」是企业内部知识库智能问答系统。员工登录后自由提问，系统从内部文档检索证据、带引用回答；管理员管文档、看缺口和看板。核心原则：宁可不答，不可错答。

旧工单副驾能力保留为「结构化直查」（`/api/lookup`），`App.vue` 旧页 D1 不删、暂不迁路由。

## 你现在是谁
你在帮我（C 板块）写代码：Vue 前端、工程骨架、部署说明、接口 mock 联调。
**不要替 A（数据与检索）和 B（生成与编排）写业务逻辑**——只接他们的接口 / 写前端 mock。

## 技术栈（不要随便换）
- 后端：Python 3.10 + FastAPI + Uvicorn + Pydantic v2 + SQLite
- 前端：Vue 3 + Vite，JS（不上 TS）
- 前端可用：Vue Router（已放开）
- 前端不用：Pinia、Element Plus / Ant Design、axios（用原生 fetch）
- token / role：存 `localStorage`，不上 Pinia
- HTTP：同步 JSON，不要 SSE/WebSocket
- LLM / LangGraph / FAISS：A/B 负责，你不要写

## 接口契约硬约束（改了就是事故）
- 权威文档：`docs/接口契约.md`（v2）、`docs/知答-完整版-开发文档.md`
- Base URL：`http://127.0.0.1:8000`
- Vite：`http://127.0.0.1:5173`
- 除 `/api/auth/login` 外，请求头带 `Authorization: Bearer <token>`
- 字段全部 snake_case；空值用 JSON `null`
- 错误统一 `{"detail": "..."}` + 4xx/5xx
- `confidence` 只有 `"high"` / `"low"`
- 角色：`admin` / `ops` / `newbie`
- 前端问答走 `POST /api/chat`，**不再调** `/api/draft`（draft 留给 B）
- `GET /api/lookup` 保留（结构化直查）
- `POST /api/feedback` 新契约：`{ message_id, useful }`

## 当前前端结构（C）
- `zhida_web/src/router/`：路由 + 守卫
- `zhida_web/src/views/`：登录、问答、管理后台
- `zhida_web/src/api/`：接口层（D1 mock，后续接真后端）
- `zhida_web/src/App.vue`：旧工单副驾页（暂不挂路由，勿删）

## 代码风格
- 注释用中文
- 函数短小，一件事一个函数
- mock 返回必须与接口契约字段一致
- Vue 用 `<script setup>`，不要 Options API
- 不要加多余抽象；需要新 npm/Python 包先问我

## 不要做的事
- 不要上 MySQL/PostgreSQL/Redis（MVP 用 SQLite）
- 不要替 A/B 写 JWT、文档解析、LangGraph、FAISS 业务
- 不要改接口字段名；要加字段走契约评审
- 不要删除 `backend/draft/` 或旧 `App.vue`
- 不要回到油猴脚本方案

## 我让你做什么时
- 先读 `docs/接口契约.md` 和相关现有文件
- 改完告诉我改了哪个文件、为什么
- 需求和契约冲突时提醒我，不要默默改契约
