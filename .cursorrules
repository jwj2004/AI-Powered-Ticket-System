# 知答 MVP · Cursor 项目规则

## 项目是什么
「知答」是一个 SaaS 售后工单智能副驾的 MVP。一线客服在网页上打开一个页面，粘贴客户问题，系统返回错误码释义、相似历史工单、可复制的答复草稿。

## 你现在是谁
你在帮我（小组里负责 C 板块）写代码。我的职责是：FastAPI 工程骨架、Vue 前端页面、接口契约、埋点上报、部署。
**不要替 A（数据检索）和 B（草稿生成）写业务逻辑**——他们有自己的分工。你只写骨架、mock 接口、前端页面，以及把 A/B 的实现接进来。

## 技术栈（不要随便换）
- 后端：Python 3.10 + FastAPI + Uvicorn + Pydantic v2
- 数据库：SQLite（MVP 阶段不要上 MySQL/PostgreSQL）
- 前端：Vue 3 + Vite，单页面，JS（不上 TS）
- 前端不引入：Vue Router、Pinia、Element Plus/Ant Design 等 UI 库、axios（用原生 fetch）
- HTTP：同步 JSON，不要 SSE/WebSocket
- LLM 调用：B 负责，你不要写调用 LLM 的代码

## 接口契约硬约束（改了就是事故）
- 后端 Base URL：`http://127.0.0.1:8000`
- 前端 Vite dev server：`http://127.0.0.1:5173`
- 字段全部 snake_case
- 空值用 JSON `null`，不要用空字符串 `""`
- 错误统一返回 `{"detail": "..."}`，HTTP 4xx/5xx，不要在 200 里塞 error 字段
- `confidence` 只有 `"high"` / `"low"` 两个值
- 三个核心接口：
  - `GET /api/lookup?code=XXX`
  - `POST /api/draft`，body `{raw_text, customer_id}`，response 必须含 `query_id`
  - `POST /api/feedback`，body `{query_id, copied, thumbs_down}`
- 内部接口 `POST /api/retrieve` 是 B 调 A 的，前端不直接调

## 当前文件
- `main.py`：FastAPI 入口，含三个 mock 接口和 CORS
- `src/App.vue`：Vue 前端主页面（输入框 + 三块结果 + 复制按钮）
- `seed_*.csv`：模拟数据（A 负责灌库，你别动）
- `接口契约.md`：接口字段定义，改字段前必须读它

## 代码风格
- 注释用中文
- 函数短小，一个函数做一件事
- mock 接口返回的数据要和接口契约完全一致，包括字段名和空值处理
- 不要加额外的抽象层、设计模式、配置文件——这是 5 天 MVP，越简单越好
- 不要引入新依赖。需要新包先问我
- Vue 组件用 `<script setup>` 语法，不要用 Options API

## 不要做的事
- 不要上 MySQL/PostgreSQL/Redis
- 不要引入 LangGraph/LangChain/FAISS/BGE（那是 A/B 的活）
- 不要上 Vue Router/Pinia/UI 库/axios
- 不要改接口字段名。要改加新字段，不要动现有字段
- 不要加用户登录、权限、审计功能（MVP 不做）
- 不要加测试框架——先把功能跑通
- 不要回到油猴脚本方案

## 我让你做什么时
- 先读 `main.py` 和 `src/App.vue` 理解现状
- 改完告诉我改了哪个文件、为什么
- 如果我的需求和接口契约冲突，提醒我，不要默默改契约
